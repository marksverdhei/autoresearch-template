[project]
name = "autoresearch"
version = "0.1.0"
description = "Autonomous agent-driven optimization template"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "pandas",
]
